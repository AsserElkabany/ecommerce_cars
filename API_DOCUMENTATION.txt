Car Ecommerce API Documentation

Base URL:
- https://localhost:5001 or http://localhost:5000

Authentication:
- POST /api/auth/login
  - Description: Authenticate a user and receive a JWT token.
  - Body: { "email": "user@task.local", "password": "User123!" }
  - Response: { "token": "...", "expiresAt": "...", "role": "User", "userId": 2 }

- POST /api/auth/signup
  - Description: Register a new user with default role User.
  - Body: { "name": "Test User", "email": "test@task.local", "password": "Test123!" }
  - Response: JWT token for the new user.

Debug:
- GET /api/debug/db-connection
  - Description: Check database connection. No auth required.
  - Response: "Database connection is OK." or error.

Public user lookup:
- GET /api/users/public/{id}
  - Description: Get a user by ID without authentication.
  - Response: User details or 404.

Admin user management:
- GET /api/users
  - Description: List all users. Admin only.
- GET /api/users/{id}
  - Description: Get a user by ID. Admin only.
- POST /api/users
  - Description: Create a new user (Admin can assign role Admin or User).
  - Body: { "name": "User Name", "email": "email@task.local", "password": "Secret123!", "role": "User" }
- POST /api/users/admin
  - Description: Create a new admin user. Admin only.
  - Body: { "name": "Admin Name", "email": "admin2@task.local", "password": "Admin123!" }
- PUT /api/users/{id}
  - Description: Update existing user details. Admin only.
- DELETE /api/users/{id}
  - Description: Delete a user. Admin only.

Car inventory:
- GET /api/cars
  - Description: List all cars. Authenticated users.
- GET /api/cars/{id}
  - Description: Get car details. Authenticated users.
- POST /api/cars
  - Description: Create a new car listing. Admin only.
  - Body: { "make": "Toyota", "model": "Camry", "description": "Sedan with low mileage", "price": 28000.00, "year": 2024, "sellerUserId": 2, "categoryIds": [1,2] }
- POST /api/cars/{id}/assign
  - Description: Assign a seller to a car. Admin only.
  - Body: { "sellerUserId": 2 }
- PUT /api/cars/{id}
  - Description: Update car details. Admin only.
- PATCH /api/cars/{id}/availability?isAvailable=true
  - Description: Mark a car as available or unavailable. Admin and User roles can use this endpoint.
- DELETE /api/cars/{id}
  - Description: Delete a car listing. Admin only.

Category management:
- GET /api/categories
  - Description: Get all categories. Authenticated users.
- POST /api/categories
  - Description: Create a new category. Admin only.
  - Body: { "name": "SUV" }

Default seeded accounts:
- Admin: admin@task.local / Admin123!
- User: user@task.local / User123!

Swagger Authorization:
1. Login via POST /api/auth/login.
2. Copy the returned token.
3. In Swagger UI, click Authorize.
4. Paste: Bearer {token}
5. Close the dialog and call protected endpoints.
